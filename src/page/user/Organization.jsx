import React, { useEffect } from "react";
import { observer } from "mobx-react";
import useStores from "~/hooks/useStores";
import {Card} from 'antd'

const Organization = () => {
  return (
    <Card size="small" title="组织">

    </Card>
  )
};

export default observer(Organization);
